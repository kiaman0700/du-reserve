# 📋 백엔드 개발자 구현 요청서 (backend_requests.md)

본 문서는 **DU-Smart Reservation** 서비스에서 프론트엔드가 우회되거나 조작되는 문제를 방어하고, 실시간 예약 만료/해제 등의 스케줄링 태스크를 완벽하게 데이터베이스 및 서버 수준에서 강제하기 위해 **백엔드(Supabase / Express API)에서 구현 및 구성해야 하는 핵심 요건**을 정리한 명세서입니다.

---

## 🛠️ 백엔드 요구 사항 (TODO List)

### 📌 [TODO 1] 15분 미입실 예약 자동 폭파 및 개방 (Scheduler / Worker)
* **목적**: 학생이 원격 예약 후 15분 이내에 입실 인증(GPS 또는 QR)을 완료하지 않은 경우, 예약을 강제 종료하고 좌석을 즉시 반납(`AVAILABLE` 상태로 전환) 처리해야 합니다.
* **구현 요건**:
  - `reservations` 테이블에 입실 인증 상태(`is_checked_in`, `check_in_at`)를 기록할 수 있는 컬럼을 추가합니다.
  - 매 분 실행되는 스케줄러(pg_cron 또는 Node.js Worker)를 가동합니다.
  - 예약 시점(`reserved_at`)으로부터 15분이 경과했음에도 `is_checked_in`이 `false`인 경우:
    1. 해당 예약의 상태를 `FORCED_RELEASED` 또는 `COMPLETED`로 변경하고 반납 시각을 기록합니다.
    2. 좌석 상태를 `AVAILABLE`로 초기화하고 예약 ID 매핑을 끊어 줍니다.

---

### 📌 [TODO 2] 10분 부재 신고 셀프 소명 만료 및 제재 자동화 (Scheduler / Worker)
* **목적**: 1차 부재 신고 접수 후 10분 이내에 학생이 셀프 소명(셀카 업로드 또는 QR 스캔)을 완료하지 않은 경우, 예약을 즉각 취소하고 해당 학생에게 경고 누적 및 이용 제재 패널티를 부여해야 합니다.
* **구현 요건**:
  - 매 분 실행되는 배치 스케줄러를 등록합니다.
  - `absence_reports`의 상태가 `PENDING`이고, 최초 신고 시각(`first_reported_at`)으로부터 10분이 초과된 건을 조회합니다.
  - 소명 기한이 초과된 대상 건에 대하여:
    1. 해당 예약 좌석을 강제 반납 처리하여 `AVAILABLE`로 전환합니다.
    2. `absence_reports` 상태를 `RESOLVED_RELEASED`로 업데이트합니다.
    3. `profiles` 테이블의 해당 학생 레코드에 경고 카운트를 추가하거나, 특정 시간 동안 예약을 차단하도록 `penalty_ends_at` 및 `penalty_reason`을 설정합니다.

---

### 📌 [TODO 3] 서버 사이드 GPS 위/경도 및 도서관 반경 검증 (API Endpoint)
* **목적**: 학생이 클라이언트 사이드 Geolocation API 스크립트나 위도/경도 변수를 인위적으로 조작(GPS 우회)하여 입실하는 행위를 물리적으로 차단합니다.
* **구현 요건**:
  - GPS 입실 인증 요청 시, 클라이언트의 현재 위도(`latitude`)와 경도(`longitude`)를 POST 요청으로 수신하는 보안 API 엔드포인트를 구축합니다.
  - 서버 내부에서 하버사인(Haversine) 공식을 활용하여 대구대학교 성산도서관 좌표(`35.9015`, `128.8488`)와의 거리를 직접 연산합니다.
  - 계산된 거리가 **50m를 초과**하는 경우, HTTP `400 Bad Request` 에러 및 인증 거부 응답을 반환하고 DB 예약을 승인하지 않습니다.

---

### 📌 [TODO 4] 악성/스팸 신고 방지 30분 쿨타임 데이터베이스 제약 (DB Constraint / Trigger)
* **목적**: 프론트엔드 코드 우회(API 직접 호출 등)를 통해 30분 이내에 악성 민원을 연속으로 접수하는 비정상적인 행위를 데이터베이스 레벨에서 원천 봉쇄합니다.
* **구현 요건**:
  - `complaints` 테이블에 데이터가 `INSERT`되기 전 실행되는 Trigger 함수를 생성합니다.
  - 동일한 `user_id`로 최근 30분 이내에 작성된 민원 레코드가 존재하는지 검증합니다.
  - 30분 이내에 접수된 이력이 있다면 `RAISE EXCEPTION`을 통해 트랜잭션을 강제로 중단시키고 삽입을 거부합니다.

---

### 📌 [TODO 5] 좌석 수동 점검중(`MAINTENANCE`) 상태 및 예약 차단 트리거 (DB Trigger)
* **목적**: 관리자가 좌석 상태를 `MAINTENANCE`로 설정했을 때, 프론트엔드를 거치지 않고 DB 레벨에서 임의로 예약을 시도하거나 변경하려는 악성 쿼리를 원천 차단합니다.
* **구현 요건**:
  - `reserve_seat` 함수(RPC) 및 `reservations` 생성 트리거에 해당 좌석의 `status`가 `'MAINTENANCE'`인 경우 예약을 원천 거부하는 비즈니스 검증 로직을 보완합니다.
  - 해당 좌석에 대한 예약 등록 요청(`INSERT`)이 오면 "해당 좌석은 현재 수동 점검 중입니다"라는 예외 에러와 함께 처리를 취소합니다.

---

### 📌 [TODO 6] 미해결 긴급 민원 실시간 피드 발행 (Supabase Realtime Sync)
* **목적**: 어드민 화면에서 실시간으로 민원이 들어오면 리프레시 없이 상태가 공유되도록 지원합니다.
* **구현 요건**:
  - `complaints` 테이블에 대해 Supabase Realtime 복제(Replication)를 활성화하여 `INSERT` 및 `UPDATE` 이벤트가 발생할 때마다 클라이언트에 실시간 브로드캐스트되도록 채널을 열어 둡니다.
